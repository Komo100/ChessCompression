#ifndef HUFFMAN_H
#define HUFFMAN_H

#include <string>
#include <iostream>
#include <map>
#include <vector>
#include <algorithm>

std::map<std::string, std::string> Huffman(std::map<std::string, int> Symbols);
    
#endif